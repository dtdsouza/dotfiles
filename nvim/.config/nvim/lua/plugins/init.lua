-- Plugin specs live in this directory. lazy.nvim imports every .lua file
-- under lua/plugins/ (see lua/config/lazy.lua -> { import = "plugins" }).
-- Each file returns a spec table. Add plugins as new files or in this list.
return {}
